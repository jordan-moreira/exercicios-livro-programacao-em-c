#include <stdio.h>

int verificar_num(int x)
{
    if (x > 0)
    {
        return 1;
    }
    else
    {
        return 0;
    }
}

int main()
{
    int num, res;

    printf("Digite um número: ");
    scanf("%d", &num);

    res =
        printf("%d \n", res = verificar_num(num));
}
